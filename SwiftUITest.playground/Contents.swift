import SwiftUI

var body: some View {

    ZStack {
        VStack(alignment: .center) {
            Text("Filter")
        }
        VStack(alignment: .trailing) {
            Text("Apply")
        }
    }

}
